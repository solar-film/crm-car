# รายงานตรวจสอบบั๊กและปัญหา — ระบบ CAR CRM

วันที่ตรวจ: 21 พ.ค. 2569 · ขอบเขต: ทุกไฟล์ในโฟลเดอร์ CAR_CRM (11 หน้า HTML + sidebar.js + apps-script.gs)

---

## สรุปภาพรวม

ตรวจพบทั้งหมด 14 ประเด็น แบ่งเป็นความรุนแรง สูง 3, ปานกลาง 6, ต่ำ 5 ประเด็นใหญ่ที่ควรแก้ก่อนคือ ช่องโหว่ XSS ในหน้า Feedback, การที่ระบบล็อกอินไม่ได้ป้องกันข้อมูลจริง และการเขียน/ลบข้อมูลผ่าน GET

---

## ระดับ: สูง

### 1. ช่องโหว่ XSS ในหน้า feedback-car.html

ไฟล์: `feedback-car.html` (บรรทัด ~385-417)

หน้านี้นำข้อมูล `d.customer` (ชื่อลูกค้า) และ `d.feedback` (ข้อเสนอแนะ) มาแสดงด้วย `card.innerHTML` โดยตรง โดยไม่มีการ escape ขณะที่หน้าอื่นทุกหน้าใช้ฟังก์ชัน `escapeHtml` ป้องกันไว้แล้ว เนื่องจากข้อมูลนี้มาจาก Google Form ที่ใครก็กรอกได้ ลูกค้า (หรือผู้ไม่หวังดี) สามารถส่งข้อความที่เป็นโค้ด HTML/JavaScript เข้ามา แล้วโค้ดจะทำงานในเบราว์เซอร์ของพนักงานที่เปิดดูหน้านี้

แนวทางแก้: สร้างฟังก์ชัน `escapeHtml` เหมือนหน้าอื่น แล้วครอบ `${d.customer}`, `${d.feedback}`, `${d.date}` ทุกจุดที่แทรกลงใน innerHTML

### 2. ระบบล็อกอินไม่ได้ป้องกันข้อมูลจริง และข้อมูลลูกค้าเปิดสาธารณะ

ไฟล์: ทุกหน้า, `apps-script.gs`

ระบบทั้งหมดอ่านข้อมูลจาก Google Sheet ผ่านลิงก์ CSV สาธารณะ (`gviz/tq?out:csv`) ซึ่งต้องตั้งค่าแชร์ชีตเป็น "ทุกคนที่มีลิงก์" และ `SHEET_ID` ฝังอยู่ในไฟล์ HTML ทุกไฟล์ ผลคือใครก็ตามที่ได้ลิงก์หน้าเว็บ (หรือกด View Source) จะเห็น Sheet ID และอ่านข้อมูลลูกค้าทั้งหมดได้ — ชื่อ เบอร์โทร ยอดขาย

หน้าจอล็อกอินไม่ได้ช่วยป้องกันจริง เพราะรหัสผ่าน `MHL741mhl+` เขียนตรงๆ ในโค้ดฝั่งหน้าเว็บ (เห็นได้จาก View Source) และสถานะล็อกอินเก็บใน `localStorage` ที่ใครก็ตั้งค่าเองได้ นอกจากนี้ Token ของ Apps Script (`mhl-2026-admin`) และ Web App URL ก็ฝังเป็นค่าเริ่มต้นใน `admin.html` ทำให้ผู้ที่ได้ไฟล์สามารถเขียน/ลบข้อมูลในชีตได้

หมายเหตุ: หากใช้เป็นเครื่องมือภายในร้านเล็กๆ อาจยอมรับความเสี่ยงนี้ได้ แต่ควรรับทราบว่าระบบนี้ไม่มีการป้องกันข้อมูลที่แท้จริง หากต้องการความปลอดภัยจริง ต้องย้าย logic การตรวจสอบสิทธิ์ไปไว้ฝั่งเซิร์ฟเวอร์ (Apps Script) และไม่เปิดชีตเป็นสาธารณะ

### 3. การเขียน/ลบข้อมูลผ่าน HTTP GET

ไฟล์: `apps-script.gs` (บรรทัด 26-61), `admin.html` (`apiRequest`)

`doGet` รองรับ action `customer.save`, `booking.save`, `customer.delete`, `booking.delete` ซึ่งเป็นการแก้ไข/ลบข้อมูล และ `admin.html` ส่งคำสั่งบันทึกทั้งหมดผ่าน GET โดยแนบ JSON ทั้งก้อนไว้ใน query string ทำให้เกิดปัญหา:

- คำสั่งทำลายข้อมูลผ่าน GET อาจถูกกระตุ้นโดยไม่ตั้งใจ (crawler, prefetch ของเบราว์เซอร์) และ Token ติดไปกับ URL จึงถูกบันทึกใน history/log/proxy
- ถ้าหมายเหตุ (`หมายเหตุ`) ยาวมาก URL อาจเกินขีดจำกัดความยาว ทำให้บันทึกไม่สำเร็จ

แนวทางแก้: ให้ `admin.html` ส่งคำสั่งบันทึก/ลบผ่าน `doPost` (โค้ด `doPost` มีอยู่แล้วใน apps-script.gs แต่ไม่เคยถูกเรียกใช้) และให้ `doGet` รองรับเฉพาะการอ่านข้อมูล

---

## ระดับ: ปานกลาง

### 4. ระบบล็อกอินไม่สอดคล้องกันระหว่างหน้า

หน้าส่วนใหญ่ (`index`, `admin`, `calendar`, `damage`, `other-damage`, `sunroof`, `sales-dashboard`) เก็บสถานะล็อกอินใน `localStorage` (คีย์ `'yes'`) แต่:

- `install-summary.html` ใช้ `sessionStorage` อย่างเดียว (คีย์ `'1'`)
- `overview.html` และ `contact-stats.html` ยอมรับทั้งสองแบบ
- `feedback-car.html` ไม่มีหน้าล็อกอินเลย

ผลคือ ผู้ใช้ที่ล็อกอินจากหน้าทั่วไปแล้วเปิด `install-summary.html` จะถูกให้ล็อกอินใหม่ และการล็อกอินที่ `install-summary` จะถูกลืมเมื่อปิดเบราว์เซอร์ ในขณะที่หน้าอื่นจำได้ถาวร

แนวทางแก้: เลือกใช้วิธีเดียวกันทุกหน้า (แนะนำให้ทุกหน้าตรวจ `localStorage===yes` หรือ `sessionStorage==='1'` และตั้งค่าทั้งคู่เหมือน overview/contact-stats) และเพิ่มหน้าล็อกอินให้ feedback-car.html

### 5. หน้า index.html ล่มทั้งหน้าถ้าชีตใดชีตหนึ่งหายหรือเปลี่ยนชื่อ

ไฟล์: `index.html` (`fetchGoogleSheetData`, บรรทัด 1121-1148)

`index.html` โหลด 6 ชีตพร้อมกันด้วย `Promise.all` ถ้าชีตใดตอบกลับไม่สำเร็จ (เช่น ถูกเปลี่ยนชื่อ) ทั้งหน้าจะขึ้น error ทันที จุดเสี่ยงที่สุดคือชื่อชีตสถิติการติดต่อ ซึ่ง `index.html` ฮาร์ดโค้ดไว้ว่า `'สถิตการติดต่อ'` (สะกดตก ิ — น่าจะเป็น `สถิติการติดต่อ`) ขณะที่หน้า `contact-stats.html` และ `overview.html` มีการเดาชื่อหลายแบบสำรองไว้ แต่ `index.html` ไม่มี

แนวทางแก้: ทำให้ index.html ทนทานต่อชีตที่ขาดหายไป (ใช้ `.catch` ต่อชีตที่ไม่จำเป็น) และยืนยันชื่อชีตสถิติให้ตรงกับชื่อจริงใน Google Sheet หรือใช้วิธีลองหลายชื่อแบบหน้าอื่น (หน้า calendar, damage, other-damage ก็มีรูปแบบ all-or-nothing แบบเดียวกัน)

### 6. ช่องค้นหาในหน้าปฏิทันไม่ทำงาน

ไฟล์: `calendar.html` (`applyFilters`, บรรทัด 1092-1123)

`applyFilters()` คำนวณ `filteredData` (กรองตามวันที่ + คำค้นหา) แต่จากนั้นเรียก `renderCalendar()` ซึ่งไปดึงข้อมูลจาก `allData` ใหม่ทั้งหมด ไม่ได้ใช้ `filteredData` เลย ผลคือพิมพ์คำค้นหาในหน้าปฏิทินแล้วไม่มีอะไรเปลี่ยน

แนวทางแก้: ให้ `renderCalendar` รับและใช้ข้อมูลที่กรองแล้ว หรือกรอง `allData` ภายใน `getJobsByDateKey` ตามคำค้นหา

### 7. ตัวเลข "คิววันนี้" ในหน้า admin คลาดเคลื่อน

ไฟล์: `admin.html` (`updateSummary`, บรรทัด 523-530)

โค้ดนับคิววันนี้ด้วยการเช็ก `String(วันที่).includes(thaiToday)` ซึ่งเป็นการหาข้อความย่อย ทำให้วันที่หลักเดียวจับซ้อนกัน เช่น ถ้าวันนี้คือ `1/5/2569` จะนับ `11/5/2569`, `21/5/2569`, `31/5/2569` รวมเข้ามาด้วย ทำให้ตัวเลขคิววันนี้สูงเกินจริง

แนวทางแก้: เปรียบเทียบแบบตรงตัว `=== thaiToday` แทนการใช้ `includes`

### 8. ช่องโหว่ JSONP callback ใน apps-script.gs

ไฟล์: `apps-script.gs` (`jsonResponse`, บรรทัด 224-236)

`jsonResponse` นำค่าพารามิเตอร์ `callback` มาต่อเข้ากับผลลัพธ์ JavaScript โดยตรง (`${callback}(${text});`) โดยไม่ตรวจสอบ ทำให้ผู้ไม่หวังดีสร้าง URL ที่มี `callback=` เป็นโค้ดอะไรก็ได้ แล้วได้ JavaScript ที่ทำงานได้กลับมา (reflected injection)

แนวทางแก้: ตรวจรูปแบบ `callback` ให้ผ่านเฉพาะ `/^[A-Za-z0-9_.]+$/` ก่อนนำมาใช้

### 9. ID ที่สร้างอัตโนมัติอาจชนกันแล้วเขียนทับข้อมูลเดิม

ไฟล์: `apps-script.gs` (`saveRow` + `generateId`, บรรทัด 156-215)

`generateId` สร้าง ID รูปแบบ `PREFIX-yymmdd-NNN` โดย NNN เป็นเลขสุ่ม 100-999 (มีแค่ 900 ค่าต่อวัน) จากนั้น `saveRow` ค้นหาแถวด้วย ID นั้นทันที ถ้าเลขสุ่มบังเอิญตรงกับ ID ของรายการเดิมในวันเดียวกัน ระบบจะ **เขียนทับรายการเดิม** แทนที่จะสร้างรายการใหม่ โดยไม่มีการเตือน

แนวทางแก้: หลังสร้าง ID ให้วนตรวจว่าซ้ำกับของเดิมหรือไม่ ถ้าซ้ำให้สุ่มใหม่ หรือใช้รูปแบบ ID ที่รับประกันความไม่ซ้ำ (เช่น เพิ่ม timestamp/running number)

---

## ระดับ: ต่ำ

### 10. ความเสี่ยง Formula Injection ใน Google Sheet

`apps-script.gs` เขียนข้อมูลที่ผู้ใช้กรอกลงเซลล์โดยตรง (`appendRow`/`setValues`) ถ้าชื่อลูกค้าหรือหมายเหตุขึ้นต้นด้วย `=`, `+`, `-`, `@` ค่านั้นจะกลายเป็นสูตรที่ทำงานจริงในชีต ควรเติมเครื่องหมายนำหน้า (เช่น `'`) ให้กับค่าที่ขึ้นต้นด้วยอักขระเหล่านี้

### 11. เบอร์โทรอาจตกเลข 0 นำหน้า

`formatCell` ใน apps-script.gs แปลงค่าด้วย `String(value)` ถ้าเซลล์เบอร์โทรถูกเก็บเป็นตัวเลข เบอร์ `0812345678` จะกลายเป็น `812345678` ควรบังคับให้คอลัมน์เบอร์โทรในชีตเป็นรูปแบบข้อความ (Plain text)

### 12. หน้า sunroof.html เข้าไม่ถึงจากเมนู

`sidebar.js` (`CAR_CRM_NAV`) ไม่มีรายการเมนูสำหรับ `sunroof.html` (แม้ `CAR_CRM_TITLES` จะมีชื่อหน้านี้อยู่) ผู้ใช้จึงไม่มีทางคลิกเข้าหน้า Sunroof จากแถบเมนูได้ นอกจากนี้ลิงก์ "Admin Panel" ในเมนูชี้ไปที่ URL ของ AppSheet ภายนอก ไม่ใช่ไฟล์ `admin.html` ในโฟลเดอร์นี้ — ควรตรวจว่าตั้งใจหรือไม่

### 13. ไม่มีการล็อกขณะเขียนข้อมูลพร้อมกัน

`apps-script.gs` ไม่ได้ใช้ `LockService` ถ้ามีการบันทึก 2 รายการพร้อมกัน อาจเกิดการชนกันของแถว ควรครอบการเขียนด้วย `LockService.getScriptLock()`

### 14. ประเด็นเล็กน้อยอื่นๆ

- `index.html` / `calendar.html`: การค้นหาข้อมูลลูกค้า (`customerMap[custId]`) ใช้ค่า ID ดิบอย่างเดียว ไม่มี fallback แบบ normalize เหมือนการค้นหา filmDetails ถ้า CustID มีช่องว่าง/ตัวพิมพ์ต่างกันเล็กน้อย ชื่อลูกค้าจะหายเป็น `-`
- `index.html`: เวลารับรถ/ส่งรถในใบพรินต์ ชั่วโมงไม่เติม 0 นำหน้า (แสดง `9:05` แทน `09:05`)
- `calendar.html`: มองหาคอลัมน์ราคาชื่อ `มูลค่าสินค้า` ซึ่งไม่มีในชีต Bookings (ชื่อจริงคือ `ยอดขาย`) ทำให้ `price` เป็น 0 เสมอ (แต่หน้าปฏิทินไม่ได้แสดงราคา จึงไม่กระทบการแสดงผล)
- `overview.html`: โหลดและบังคับว่าต้องมีชีต Customer แต่ไม่ได้นำข้อมูลลูกค้าไปใช้เลย
- `other-damage.html`: `searchText` มีคำว่า `[object Object]` ปนเพราะ join ออบเจ็กต์ `raw` เข้าไปด้วย
- `apps-script.gs`: `doPost` ส่งเลขสถานะ `400`/`500` เข้า `jsonResponse` แต่ฟังก์ชันไม่รองรับ จึงถูกละเลย (ไม่กระทบการทำงาน แต่เป็นโค้ดที่ทำให้เข้าใจผิด)
- รหัสผ่าน `MHL741mhl+` ถูกเขียนซ้ำใน 9 ไฟล์ การเปลี่ยนรหัสต้องแก้ทุกไฟล์

---

## ข้อเสนอแนะ — ลำดับการแก้

ควรแก้เรียงตามนี้: (1) escape ข้อมูลในหน้า feedback-car เพื่อปิดช่องโหว่ XSS, (2) ปรับ admin ให้บันทึกผ่าน doPost, (3) ทำให้ระบบล็อกอินสอดคล้องกันทุกหน้า, (4) แก้ตัวนับคิววันนี้และช่องค้นหาในหน้าปฏิทัน ซึ่งเป็นบั๊กที่ผู้ใช้เห็นผลโดยตรง, (5) ทำให้ index.html ทนทานต่อชีตที่ขาดหาย จากนั้นจึงพิจารณาประเด็นความปลอดภัยเชิงโครงสร้าง (การเปิดชีตสาธารณะ) ตามความเหมาะสมของการใช้งานจริง
